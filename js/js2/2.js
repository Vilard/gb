// 2. чему равны x и a в примере, почему?(описать)
let a = 2
let x = 1 + (a *= 2)

// a == 4
// x == 5
// при выполнении кода есть а *= 2, это можно записать как а = 2 * а, 
// мы переопределяем переменную а, после чего прибавляем 1
